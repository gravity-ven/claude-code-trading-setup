====================================================================
   SPARTAN 100 COT AGENTS - QUICKSTART GUIDE
====================================================================

✅ INSTALLATION COMPLETE!

The COT (Commitment of Traders) agent system has been integrated
into the Spartan Research Station.

--------------------------------------------------------------------
STEP 1: Initialize the Database
--------------------------------------------------------------------

Run this ONCE to create the database tables:

psql -d spartan_research_db -U spartan -f agents/cot_agents/schema/cot_agents_schema.sql

You should see:
  ✅ Spartan 100 COT Agents schema initialized successfully!
  📊 Tables created: 9
  🤖 Agent registry: 100 agents initialized

--------------------------------------------------------------------
STEP 2: Test the System
--------------------------------------------------------------------

Run a quick demo to verify everything works:

python3 run_100_agents.py --demo --single-cycle

Expected output:
  🚀 Initializing Spartan 100 Agent System
  📊 DEMO MODE: Initializing minimal agent subset
  ✅ Initialized 4 agents
  🚀 STARTING SINGLE CYCLE
  ✅ CYCLE COMPLETED in X.XX seconds

--------------------------------------------------------------------
STEP 3: Install for Auto-Start (Optional)
--------------------------------------------------------------------

To run agents automatically on boot:

sudo ./install_agents_service.sh

This creates a systemd service that:
- Starts on boot
- Runs continuously (every hour)
- Saves trade sheets to output/latest_trade_sheet.txt

Control commands:
  sudo systemctl start spartan_agents    # Start
  sudo systemctl stop spartan_agents     # Stop
  sudo systemctl status spartan_agents   # Check status

--------------------------------------------------------------------
USAGE MODES
--------------------------------------------------------------------

1. DEMO MODE (Quick Test)
   python3 run_100_agents.py --demo

2. SINGLE CYCLE (Run Once)
   python3 run_100_agents.py --single-cycle

3. CONTINUOUS (Runs Every Hour)
   python3 run_100_agents.py

4. CUSTOM INTERVAL
   python3 run_100_agents.py --interval 2  # Every 2 hours

--------------------------------------------------------------------
MONITORING
--------------------------------------------------------------------

View live agent output:
  ./view_agent_output.sh

  OR

  tail -f logs/agents.log

View latest trade sheet:
  cat output/latest_trade_sheet.txt

--------------------------------------------------------------------
ARCHITECTURE
--------------------------------------------------------------------

4-Tier System (100 Agents):

Tier 1 (Agents 1-30):   COT Index calculation
Tier 2 (Agents 31-55):  Seasonality analysis
Tier 3 (Agents 56-80):  Confluence models
Tier 4 (Agents 81-100): Trade sheets

Data Flow:
  CFTC.gov → COT Index → Seasonality → Confluence → Trade Sheet

--------------------------------------------------------------------
DATABASE TABLES
--------------------------------------------------------------------

After initialization, these tables will exist:

- cot_raw_data             (CFTC reports)
- cot_calculated_metrics   (COT Index)
- seasonal_patterns        (Seasonality)
- agent_signals            (Individual signals)
- confluence_scores        (Aggregated scores)
- trade_sheets             (Daily trade sheets)
- agent_performance        (Agent health)
- system_logs              (Event logs)

Query examples:
  psql -d spartan_research_db -U spartan -c "SELECT * FROM v_latest_cot_signals;"
  psql -d spartan_research_db -U spartan -c "SELECT * FROM v_active_opportunities;"
  psql -d spartan_research_db -U spartan -c "SELECT * FROM v_agent_health;"

--------------------------------------------------------------------
FILES CREATED
--------------------------------------------------------------------

agents/cot_agents/
├── base/                      (Base classes for 4 tiers)
├── schema/                    (PostgreSQL schema)
├── tier1_cot/                 (COT agents)
├── tier2_seasonal/            (Seasonal agents)
├── tier3_models/              (Model agents)
├── tier4_risk/                (Risk agents)
├── orchestrator/              (Coordination)
└── utils/                     (Utilities)

run_100_agents.py              (Main orchestrator)
install_agents_service.sh      (systemd installer)
view_agent_output.sh           (Log viewer)

output/                        (Generated trade sheets)
logs/                          (Agent logs)

COT_AGENTS_README.md           (Full documentation)

--------------------------------------------------------------------
SUPPORTED SYMBOLS (60+)
--------------------------------------------------------------------

Indices:   ES, NQ, YM, RTY, VX
Currencies: DX, EUR, JPY, GBP, AUD, CAD, CHF
Metals:    GC, SI, HG, PL
Energy:    CL, NG, HO, RB
Grains:    ZC, ZS, ZW, ZL, ZM
Softs:     SB, KC, CC, CT, OJ
Meats:     LE, GF, HE
Bonds:     ZN, ZB, ZF, ZT
Crypto:    BTC, ETH

--------------------------------------------------------------------
CRITICAL PHILOSOPHY
--------------------------------------------------------------------

✅ NO FAKE DATA - Returns null on failure, never generates mock data
✅ REAL CFTC.gov reports only
✅ PostgreSQL ONLY (no SQLite)
✅ Autonomous operation with graceful error handling

--------------------------------------------------------------------
TROUBLESHOOTING
--------------------------------------------------------------------

Database tables don't exist?
→ Run: psql -d spartan_research_db -U spartan -f agents/cot_agents/schema/cot_agents_schema.sql

Agents not running?
→ Check: sudo systemctl status spartan_agents
→ Logs: tail -f logs/agents.log

No trade sheet generated?
→ Normal on first run (need COT data from CFTC.gov)
→ CFTC updates weekly on Fridays

Connection error?
→ Check PostgreSQL is running: pg_isready
→ Check database exists: psql -l | grep spartan_research_db

--------------------------------------------------------------------
NEXT STEPS
--------------------------------------------------------------------

1. Initialize database (STEP 1 above)
2. Test with demo mode
3. (Optional) Install systemd service
4. Wait for CFTC data (Fridays 3:30 PM ET)
5. Review trade sheets in output/

Full documentation: COT_AGENTS_README.md

====================================================================
Last Updated: November 28, 2025
Status: Beta - Demo mode functional
====================================================================
